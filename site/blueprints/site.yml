title: Site

columns:
  main:
    width: 2/3
    sections:
      pages:
        type: pages
  sidebar:
    width: 1/3
    sections:
      metafields:
        type: fields
        fields:
          description:
            type: textarea
            size: small
            label: Meta Description (site-wide fallback)
            help: "Used on pages that have no own description. Recommended: 120–160 characters."
